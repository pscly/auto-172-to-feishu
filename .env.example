# 示例环境变量模板：将此文件复制为 .env 并填写真实值（不要把 .env 提交到仓库）
# 说明：所有注释仅供参考，实际使用时请替换为你的真实值或保留空字符串以明确需要填写的字段。

# 商城/产品页面（可选）
172_index_url="https://h5.lot-ml.com/ProductEn/Index/ad6c8ab0079c1140"

# 飞书多维表 (Bitable) 配置说明
# FS_app_token: 填写「数据表 ID / Base ID」，即飞书表格 URL 中的那段 ID（例如 CrxTb4kzgazMEasEoM0cAcGvnff）。
# 说明：此变量用于指定要写入的目标数据表（表 ID），不是应用的 app_token（应用令牌）。
# 如果你需要填写应用级的 app_token（应用令牌），请在 `FS_APP_TOKEN`（大写） 中填写（可选）。
FS_app_token="CrxTb4kzgazMEasEoM0cAcGvnff"
# 飞书应用凭证（在飞书开发者后台获取）
FS_APP_ID="your_fs_app_id_here"
FS_APP_SECRET="your_fs_app_secret_here"
# 可选：飞书 API 授权（如果使用其它授权方式时）
FS_Authorization=""

# 抓包获取的 API 授权头（用于请求产品数据，需包含 Bearer 前缀）
AUTHORIZATION="Bearer <your_token_here>"
# 抓包或接口文档中需要的 CID（可选）
CID="<your_cid_here>"

# 用户名和密码 更推荐
USERNAME=''
PASSWORD=''

# 折扣率，决定返现金额的比例（示例：0.9 表示按 90% 计算）
ZKL=0.9

# 可覆盖的产品 API 地址（当默认地址不可用时可替换）
PRODUCTS_API_URL="https://172appapi.lot-ml.com/api/Products/NewQuery2?TimeType=0&page=1&OnShop=true&limit=199&IsKuan=0"
